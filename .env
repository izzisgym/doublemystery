# Database - Railway will inject this. For local dev, override in .env.local
DATABASE_URL="postgresql://user:password@localhost:5432/blindbox"

# Admin basic auth credentials (set real values in Railway variables)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me
